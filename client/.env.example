# 复制为 .env 后填写实际值（Vite 环境变量必须以 VITE_ 开头）

# 后端 API 基础地址
# 开发环境留空即可（默认走 /api 由 vite proxy 转发）
# 生产环境填后端域名，例如：VITE_API_BASE=https://chisha.example.com/api
VITE_API_BASE=

# 静态资源（图片等）基础地址
# 同域部署可留空；小程序或跨域部署时填后端域名，例如：VITE_ASSET_BASE=https://chisha.example.com
VITE_ASSET_BASE=
